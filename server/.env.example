# Never commit a real Tavily key.
TAVILY_API_KEY=tvly-your-key-here
# Optional deployment guard for trusted backend callers. Keep it outside source control.
VYNNRA_SERVER_TOKEN=replace-with-a-random-server-secret
PORT=8787
